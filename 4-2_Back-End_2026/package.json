{
  "name": "back-end",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Tervo-Ridor Kirill",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build:ui": "@powershell Remove-Item -Recurse -Force dist && cd ../Front-End/General-Vite-Project && npm run build && @powershell Copy-Item dist -Recurse ../../Back-End",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:ui && npm run deploy",
    "logs:prod": "fly logs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "flyctl": "^1.2.10",
    "mongoose": "^9.9.1"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
